kind: pipeline

steps:
- name: compile
  image: microsoft/dotnet:sdk
  commands:
    - sh build.sh
- name: build-image
  image: plugins/docker:18
  command:
    - docker build -t bumblefeng/dotnetcore-container ./Web