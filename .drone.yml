kind: pipeline

steps:
- name: compile
  image: microsoft/aspnetcore-build
  commands:
    - sh build.sh
- name: build-image
  image: plugins/docker:18
  command:
    - docker build -t bumblefeng/dotnetcore-container ./Web